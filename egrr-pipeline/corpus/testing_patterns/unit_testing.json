{
    "id": "test-unit-001",
    "code": "import pytest\nfrom typing import List\n\n# Example function to test\ndef divide(a: float, b: float) -> float:\n    if b == 0:\n        raise ValueError(\"Cannot divide by zero\")\n    return a / b\n\n# EGRR_PATTERN: Basic test structure with setup and assertions\nclass TestDivide:\n    \"\"\"EGRR_PATTERN: Test class with comprehensive test cases.\"\"\"\n    \n    def test_divide_positive_numbers(self):\n        \"\"\"Test basic division with positive numbers.\"\"\"\n        assert divide(10, 2) == 5.0\n        assert divide(7, 2) == 3.5\n    \n    def test_divide_negative_numbers(self):\n        \"\"\"Test division with negative numbers.\"\"\"\n        assert divide(-10, 2) == -5.0\n        assert divide(10, -2) == -5.0\n        assert divide(-10, -2) == 5.0\n    \n    def test_divide_by_zero_raises_error(self):\n        \"\"\"Test that division by zero raises ValueError.\"\"\"\n        with pytest.raises(ValueError, match=\"Cannot divide by zero\"):\n            divide(10, 0)\n    \n    def test_divide_zero_dividend(self):\n        \"\"\"Test zero divided by any number.\"\"\"\n        assert divide(0, 5) == 0.0\n        assert divide(0, -5) == 0.0\n    \n    def test_divide_float_precision(self):\n        \"\"\"Test floating point precision.\"\"\"\n        result = divide(1, 3)\n        assert abs(result - 0.333333) < 0.001\n    \n    @pytest.mark.parametrize(\"a,b,expected\", [\n        (10, 2, 5.0),\n        (9, 3, 3.0),\n        (7, 2, 3.5),\n        (1, 4, 0.25),\n    ])\n    def test_divide_parametrized(self, a: float, b: float, expected: float):\n        \"\"\"EGRR_PATTERN: Parametrized test for multiple inputs.\"\"\"\n        assert divide(a, b) == expected",
    "language": "python",
    "explanation": "Comprehensive unit testing patterns with pytest: basic assertions, exception testing, floating point comparison, and parametrized tests.",
    "tags": ["testing", "pytest", "unit-test", "assertions", "parametrize"],
    "error_signatures": ["test", "unit test", "pytest", "assertion", "test case"]
}
