{
    "id": "test-fixture-001",
    "code": "import pytest\nimport tempfile\nimport sqlite3\nfrom pathlib import Path\nfrom typing import Generator\n\n# EGRR_PATTERN: Pytest fixtures for reusable test setup\n\n@pytest.fixture\ndef sample_data() -> dict:\n    \"\"\"EGRR_PATTERN: Simple data fixture.\"\"\"\n    return {\n        'users': [\n            {'id': 1, 'name': 'Alice', 'email': 'alice@test.com'},\n            {'id': 2, 'name': 'Bob', 'email': 'bob@test.com'},\n        ],\n        'products': [\n            {'id': 1, 'name': 'Widget', 'price': 9.99},\n        ]\n    }\n\n@pytest.fixture\ndef temp_file() -> Generator[Path, None, None]:\n    \"\"\"EGRR_PATTERN: Temporary file fixture with cleanup.\"\"\"\n    with tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False) as f:\n        f.write('test content')\n        temp_path = Path(f.name)\n    \n    yield temp_path\n    \n    # Cleanup after test\n    if temp_path.exists():\n        temp_path.unlink()\n\n@pytest.fixture\ndef temp_directory() -> Generator[Path, None, None]:\n    \"\"\"EGRR_PATTERN: Temporary directory fixture.\"\"\"\n    with tempfile.TemporaryDirectory() as tmpdir:\n        yield Path(tmpdir)\n\n@pytest.fixture\ndef db_connection() -> Generator[sqlite3.Connection, None, None]:\n    \"\"\"EGRR_PATTERN: Database connection fixture with transaction rollback.\"\"\"\n    conn = sqlite3.connect(':memory:')\n    conn.execute('CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)')\n    conn.execute(\"INSERT INTO users VALUES (1, 'Test User')\")\n    conn.commit()\n    \n    yield conn\n    \n    conn.close()\n\n@pytest.fixture(scope='module')\ndef expensive_resource():\n    \"\"\"EGRR_PATTERN: Module-scoped fixture for expensive setup.\"\"\"\n    # This runs once per module, not per test\n    resource = {'initialized': True, 'data': load_large_dataset()}\n    yield resource\n    # Cleanup\n\ndef load_large_dataset():\n    return list(range(10000))\n\n# Using fixtures in tests\nclass TestWithFixtures:\n    \n    def test_with_sample_data(self, sample_data):\n        \"\"\"Test using sample_data fixture.\"\"\"\n        assert len(sample_data['users']) == 2\n        assert sample_data['users'][0]['name'] == 'Alice'\n    \n    def test_with_temp_file(self, temp_file):\n        \"\"\"Test using temporary file.\"\"\"\n        content = temp_file.read_text()\n        assert content == 'test content'\n    \n    def test_with_db(self, db_connection):\n        \"\"\"Test using database connection.\"\"\"\n        cursor = db_connection.execute('SELECT name FROM users WHERE id = 1')\n        row = cursor.fetchone()\n        assert row[0] == 'Test User'\n    \n    def test_combining_fixtures(self, temp_directory, sample_data):\n        \"\"\"EGRR_PATTERN: Combine multiple fixtures.\"\"\"\n        data_file = temp_directory / 'data.txt'\n        data_file.write_text(str(sample_data))\n        assert data_file.exists()",
    "language": "python",
    "explanation": "Pytest fixtures for reusable test setup: temporary files/directories, database connections, scoped fixtures, and proper cleanup patterns.",
    "tags": ["testing", "pytest", "fixtures", "setup", "teardown", "test-data"],
    "error_signatures": ["fixture", "test setup", "pytest fixture", "test data", "setup"]
}
